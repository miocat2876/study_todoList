<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.famback.fam.common.mapper.CommonMapper">

    <!--  공통코드 조회 다건  -->
    <select id="commonList" resultType="commonDomain">
         SELECT NUM_PK             AS commonNumPk
              , CODE_GROUP         AS codeGroup
              , CODE               AS commonCode
              , DESCRIPTION        AS commonDescription
              , CREATE_DATE        AS commonCreateDate
              , USE_YN             AS commonUseYn
           FROM COMMON_CODE_TAB
          WHERE USE_YN = 'Y'
    </select>

        <!--  공통코드 조회 다건  -->
    <select id="findByCommonCode" parameterType="commonDomain" resultType="commonDomain">
         SELECT NUM_PK             AS commonNumPk
              , CODE_GROUP         AS codeGroup
              , CODE               AS commonCode
              , DESCRIPTION        AS commonDescription
              , CREATE_DATE        AS commonCreateDate
              , USE_YN             AS commonUseYn
           FROM COMMON_CODE_TAB
          WHERE USE_YN = 'Y'
          AND CODE_GROUP IN
        <foreach item="item" index="index" collection="codeKeys"
          open="(" separator="," close=")" nullable="true">
          #{item}
        </foreach>
    </select>
</mapper>